"use client";

/**
 * Main chat window — displays message history and input.
 *
 * Placeholder scaffold. Will manage conversation state and delegate
 * API calls to lib/api.ts once business logic is implemented.
 */

export function ChatWindow() {
  return (
    <div>
      <p>Chat interface coming soon.</p>
    </div>
  );
}
