"""Service layer — application use cases.

Orchestrates domain logic, prompt assembly, memory retrieval, model
inference, and persistence. Services are the only layer that coordinates
across multiple infrastructure components.
"""
